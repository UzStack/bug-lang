class Routes(){
    func init(){
        this.rr = {
            "name": "sadas",
        };
    }

    func register(path, fn, ds){
        this.rr.add(path, fn);
    }

    func list(){
        return this.rr;
    }
}